{% comment %}
  Renders a basic embedded Giftnote form inside the product form.

  Accepts:
  - product: {Object} product object
  - section: {Object} section to which this snippet belongs
  - block: {Object} block to which this snippet belongs
{% endcomment %}

<script
  src="{{ 'giftnote-embedded-form.js' | asset_url }}"
  type="module"
  fetchpriority="low"
></script>

{% liquid
  assign storage_mode = 'cart-attributes'

  if product.gift_card?
    assign storage_mode = 'line-item-properties'
  endif
%}

<giftnote-embedded-form
  class="giftnote-embed"
  data-storage-mode="{{ storage_mode }}"
  data-default-from="{{ shop.name | escape }}"
>
  <fieldset class="giftnote-embed__toggle" aria-label="Purchase type">
    <legend class="visually-hidden">Purchase type</legend>

    <label class="giftnote-embed__toggle-option">
      <input
        type="radio"
        name="giftnote-toggle-{{ block.id }}"
        value="self"
        checked
        data-ref="mode"
      >
      <span>For Myself</span>
    </label>

    <label class="giftnote-embed__toggle-option">
      <input
        type="radio"
        name="giftnote-toggle-{{ block.id }}"
        value="gift"
        data-ref="mode"
      >
      <span class="giftnote-embed__toggle-label">
        <span class="giftnote-embed__toggle-icon" aria-hidden="true">{{- 'heart.svg' | inline_asset_content -}}</span>
        <span>Gift a Copy</span>
      </span>
    </label>
  </fieldset>

  <div class="giftnote-embed__panel" data-ref="panel" hidden>
  <div class="giftnote-embed__grid giftnote-embed__grid--two">
    <div class="field">
      <label class="visually-hidden" for="Giftnote-to-{{ block.id }}">To</label>
      <input
        id="Giftnote-to-{{ block.id }}"
        class="input giftnote-embed__input"
        type="text"
        placeholder="Recipient's name"
        maxlength="100"
        autocomplete="name"
        data-ref="to"
      >
    </div>

    <div class="field">
      <label class="visually-hidden" for="Giftnote-from-{{ block.id }}">From</label>
      <input
        id="Giftnote-from-{{ block.id }}"
        class="input giftnote-embed__input"
        type="text"
        placeholder="Your name"
        maxlength="100"
        autocomplete="name"
        data-ref="from"
      >
    </div>
  </div>

  <div class="field giftnote-embed__message-field">
    <label class="visually-hidden" for="Giftnote-message-{{ block.id }}">Message</label>
    <textarea
      id="Giftnote-message-{{ block.id }}"
      class="input textarea giftnote-embed__input giftnote-embed__textarea"
      rows="5"
      maxlength="210"
      placeholder="Personal message (optional)"
      data-ref="message"
    ></textarea>
    <span class="giftnote-embed__character-count" data-ref="messageCount">0/210</span>
  </div>

  <div class="giftnote-embed__grid giftnote-embed__grid--two">
    <div class="field">
      <label class="visually-hidden" for="Giftnote-email-{{ block.id }}">Recipient's email</label>
      <input
        id="Giftnote-email-{{ block.id }}"
        class="input giftnote-embed__input"
        type="email"
        placeholder="Recipient's email"
        inputmode="email"
        autocomplete="email"
        autocorrect="off"
        autocapitalize="off"
        data-ref="email"
      >
    </div>

    <div class="field">
      <label class="visually-hidden" for="Giftnote-phone-{{ block.id }}">Recipient's phone number</label>
      <input
        id="Giftnote-phone-{{ block.id }}"
        class="input giftnote-embed__input"
        type="tel"
        placeholder="Recipient's phone number"
        inputmode="tel"
        autocomplete="tel"
        data-ref="phone"
      >
    </div>
  </div>

  <div class="giftnote-embed__timing">
    <div class="giftnote-embed__timing-options" role="radiogroup" aria-label="When should we send your message?">
      <label class="giftnote-embed__timing-option">
        <input type="radio" name="giftnote-method-{{ block.id }}" value="tracked" data-ref="method">
        <span>On Delivery</span>
      </label>
      <label class="giftnote-embed__timing-option">
        <input type="radio" name="giftnote-method-{{ block.id }}" value="instant" checked data-ref="method">
        <span>Send Now</span>
      </label>
      <label class="giftnote-embed__timing-option">
        <input type="radio" name="giftnote-method-{{ block.id }}" value="scheduled" data-ref="method">
        <span>Send Later</span>
      </label>
    </div>

    <p class="giftnote-embed__timing-copy" data-ref="methodHelp">
      Your message will be sent right away after checkout.
    </p>

    <div class="field" data-ref="scheduleField" hidden>
      <label class="visually-hidden" for="Giftnote-time-{{ block.id }}">Scheduled send time</label>
      <input
        id="Giftnote-time-{{ block.id }}"
        class="input giftnote-embed__input"
        type="datetime-local"
        data-ref="time"
      >
    </div>
  </div>

  <p class="giftnote-embed__note">
    <span class="giftnote-embed__note-icon" aria-hidden="true">✧</span>
    <span>We'll send a beautiful email letting them know about your thoughtful gift.</span>
  </p>

  <button class="giftnote-embed__save" type="button" data-ref="saveButton">Save message</button>

  <div class="giftnote-embed__status" data-ref="status" hidden></div>
  </div>

  {% if storage_mode == 'line-item-properties' %}
    <input type="hidden" name="properties[giftnote_to]" data-hidden-field="giftnote_to">
    <input type="hidden" name="properties[giftnote_from]" data-hidden-field="giftnote_from">
    <input type="hidden" name="properties[giftnote_message]" data-hidden-field="giftnote_message">
    <input type="hidden" name="properties[giftnote_medium]" data-hidden-field="giftnote_medium">
    <input type="hidden" name="properties[giftnote_email]" data-hidden-field="giftnote_email">
    <input type="hidden" name="properties[giftnote_phone]" data-hidden-field="giftnote_phone">
    <input type="hidden" name="properties[giftnote_method]" data-hidden-field="giftnote_method">
    <input type="hidden" name="properties[giftnote_time]" data-hidden-field="giftnote_time">
  {% endif %}
</giftnote-embedded-form>

{% stylesheet %}
  .giftnote-embed {
    display: grid;
    gap: 0.95rem;
    width: 100%;
    margin-block-end: calc(var(--gap) * 2);
  }

  .giftnote-embed__toggle {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.22rem;
    margin: 0;
    padding: 0.22rem;
    border: 0;
    border-radius: 999px;
    background: linear-gradient(180deg, #f8f1e7 0%, #f3ebdf 100%);
    box-shadow: inset 0 0 0 1px rgb(232 223 211 / 0.9);
  }

  .giftnote-embed__toggle-option {
    position: relative;
    display: flex;
  }

  .giftnote-embed__toggle-option input {
    position: absolute;
    inset: 0;
    opacity: 0;
    cursor: pointer;
  }

  .giftnote-embed__toggle-option > span {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.55rem;
    width: 100%;
    min-height: 3rem;
    padding: 0.6rem 0.85rem;
    border-radius: 999px;
    color: #65727d;
    font-size: 0.98rem;
    font-weight: 600;
    line-height: 1;
    text-align: center;
    transition: background-color var(--animation-speed) var(--animation-easing),
      color var(--animation-speed) var(--animation-easing),
      box-shadow var(--animation-speed) var(--animation-easing);
  }

  .giftnote-embed__toggle-option:has(input:checked) > span {
    background: #fff;
    color: #333c44;
    box-shadow:
      inset 0 0 0 2px rgb(122 127 132 / 0.75),
      0 10px 24px rgb(218 204 188 / 0.26);
  }

  .giftnote-embed__toggle-option:has(input:focus-visible) > span {
    outline: 2px solid #b6becb;
    outline-offset: 2px;
  }

  .giftnote-embed__toggle-label {
    display: inline-flex;
  }

  .giftnote-embed__toggle-icon {
    display: inline-flex;
    width: 0.95rem;
    height: 0.95rem;
  }

  .giftnote-embed__toggle-icon svg {
    width: 100%;
    height: 100%;
  }

  .giftnote-embed__panel {
    display: grid;
    gap: 0.95rem;
    padding: 1.4rem;
    border-radius: 1.2rem;
    background:
      radial-gradient(circle at top center, rgb(255 255 255 / 0.72), transparent 62%),
      linear-gradient(180deg, #f8f4ed 0%, #f5f0e8 100%);
    box-shadow: inset 0 0 0 1px rgb(230 222 210 / 0.75);
  }

  .giftnote-embed__panel[hidden] {
    display: none !important;
  }

  .giftnote-embed__grid {
    display: grid;
    gap: 0.9rem;
  }

  .giftnote-embed__grid--two {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .giftnote-embed__input {
    display: block;
    width: 100%;
    border: 1px solid #d9dfe8;
    border-radius: 1rem;
    background: #fff;
    box-sizing: border-box;
    box-shadow: 0 2px 6px rgb(220 213 202 / 0.12);
  }

  .giftnote-embed__input.input,
  .giftnote-embed__input.textarea {
    padding: 0.92rem 1rem;
    font-size: 1rem;
    line-height: 1.35;
    color: #63707c;
  }

  .giftnote-embed__input::placeholder {
    color: #9aa4b4;
    opacity: 1;
  }

  .giftnote-embed__textarea {
    min-height: 7.75rem;
    resize: vertical;
    padding-bottom: 1.9rem;
  }

  .giftnote-embed__message-field {
    position: relative;
  }

  .giftnote-embed__character-count {
    position: absolute;
    right: 0.95rem;
    bottom: 0.78rem;
    color: #b1b8c3;
    font-size: 0.78rem;
    line-height: 1;
    pointer-events: none;
  }

  .giftnote-embed__timing {
    display: grid;
    gap: 0;
  }

  .giftnote-embed__timing-options {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0;
  }

  .giftnote-embed__timing-options,
  .giftnote-embed__timing-copy,
  .giftnote-embed [data-ref='scheduleField'] {
    display: none !important;
  }

  .giftnote-embed__timing-option {
    position: relative;
    display: flex;
  }

  .giftnote-embed__timing-option input {
    position: absolute;
    inset: 0;
    opacity: 0;
    cursor: pointer;
  }

  .giftnote-embed__timing-option span {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    min-height: 2.55rem;
    padding: 0.75rem 0.5rem;
    border: 1px solid #d9dfe8;
    background: #fff;
    color: #8b939c;
    font-size: 0.95rem;
    text-align: center;
  }

  .giftnote-embed__timing-option:first-child span {
    border-radius: 0.65rem 0 0 0.65rem;
  }

  .giftnote-embed__timing-option:last-child span {
    border-radius: 0 0.65rem 0.65rem 0;
  }

  .giftnote-embed__timing-option + .giftnote-embed__timing-option span {
    border-left: 0;
  }

  .giftnote-embed__timing-option:has(input:checked) span {
    background: #4f81ee;
    border-color: #4f81ee;
    color: #fff;
  }

  .giftnote-embed__timing-copy {
    margin: 0;
    padding: 0.9rem 1rem;
    border: 1px solid #d9dfe8;
    border-radius: 0.65rem;
    background: rgb(255 255 255 / 0.75);
    color: #6f7881;
    font-size: 0.94rem;
    line-height: 1.4;
    text-align: center;
  }

  .giftnote-embed__note {
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
    margin: 0;
    color: #67727c;
    font-size: 0.94rem;
    line-height: 1.35;
    font-weight: 500;
  }

  .giftnote-embed__note-icon {
    color: #f0ca54;
    font-size: 1.12rem;
    line-height: 1;
    transform: translateY(0.05rem);
  }

  .giftnote-embed__save {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    min-height: 3rem;
    padding: 0.9rem 1rem;
    border: 0;
    border-radius: 0.65rem;
    background: #4f81ee;
    color: #fff;
    font-size: 1rem;
    cursor: pointer;
  }

  .giftnote-embed__status {
    padding: 0.85rem 1rem;
    border-radius: 0.65rem;
    font-size: 0.9rem;
    line-height: 1.4;
  }

  .giftnote-embed__status[data-status='success'] {
    color: #215f39;
    background: rgb(33 95 57 / 0.08);
  }

  .giftnote-embed__status[data-status='error'] {
    color: rgb(var(--color-error-rgb));
    background: rgb(var(--color-error-rgb) / 0.08);
  }

  @media screen and (max-width: 749px) {
    .giftnote-embed {
      gap: 0.8rem;
    }

    .giftnote-embed__toggle {
      padding: 0.2rem;
      gap: 0.24rem;
    }

    .giftnote-embed__toggle-option > span {
      min-height: 2.7rem;
      padding: 0.55rem 0.65rem;
      gap: 0.36rem;
      font-size: 0.82rem;
    }

    .giftnote-embed__toggle-icon {
      width: 0.82rem;
      height: 0.82rem;
    }

    .giftnote-embed__panel {
      padding: 1rem;
      border-radius: 1rem;
    }

    .giftnote-embed__grid--two,
    .giftnote-embed__timing-options {
      grid-template-columns: 1fr;
    }

    .giftnote-embed__input.input,
    .giftnote-embed__input.textarea {
      padding: 0.85rem 0.9rem;
      font-size: 0.96rem;
    }

    .giftnote-embed__textarea {
      min-height: 6.75rem;
    }

    .giftnote-embed__timing-option:first-child span,
    .giftnote-embed__timing-option:last-child span {
      border-radius: 0.65rem;
    }

    .giftnote-embed__timing-option + .giftnote-embed__timing-option span {
      border-left: 1px solid #d9dfe8;
      border-top: 0;
    }

    .giftnote-embed__note {
      font-size: 0.88rem;
    }
  }
{% endstylesheet %}
